/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
      domains: ['assets.aceternity.com'], // Tambahkan domain eksternal yang diperbolehkan
    },
  };
  
  export default nextConfig;
  